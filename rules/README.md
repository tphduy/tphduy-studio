# rules/

Claude Code-only — copied into `~/.claude/rules` per the top-level README's setup step 4.

Codex has no per-path rule injection, only a repo-wide `AGENTS.md`, so nothing here is ported to Codex.
